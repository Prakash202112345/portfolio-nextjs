import React from 'react'

export const metadata = {
  title: "Prakash Sharma - About",
};

function AboutLayout({ children }) {
  return (
    <div>
      {children}
    </div>
  )
}

export default AboutLayout
