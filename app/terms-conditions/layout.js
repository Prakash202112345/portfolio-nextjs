import React from 'react'

export const metadata = {
  title: "Prakash Sharma - Terms & Conditions",
};

function Contact({ children }) {
  return (
    <div>
      {children}
    </div>
  )
}

export default Contact
