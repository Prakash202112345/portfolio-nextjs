import React from 'react'

export const metadata = {
  title: "Prakash Sharma - Contact",
};

function Contact({ children }) {
  return (
    <div>
      {children}
    </div>
  )
}

export default Contact
